import Modal from "./Modal"

export default function EditMessageModal({ isVisible, setIsVisible }) {
    return (
        <div>
            <Modal
                isVisible={isVisible}
                setIsVisible={setIsVisible}
            >
                
            </Modal> 
        </div>
    )
}
